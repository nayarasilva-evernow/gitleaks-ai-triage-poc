# Setup

## Variáveis de ambiente

Copie `.env.example` para `.env` e preencha os valores reais.

Exemplos (não use em produção):

```bash
export OPENAI_API_KEY=sk-proj-EXAMPLE_DO_NOT_USE_THIS_KEY_1234567890
export GITHUB_TOKEN=ghp_exampleTokenForDocumentationOnly0001
```

A chave acima é apenas documentação.
